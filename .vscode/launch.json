{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach to Chrome",
      "port": 9222,
      "request": "attach",
      "type": "chrome",
      "webRoot": "${workspaceFolder}"
    },
    {
      "command": "npm run dev",
      "name": "Launch via NPM",
      "request": "launch",
      "type": "node-terminal"
    }
  ]
}
